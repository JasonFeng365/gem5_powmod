let {{
    class UInt64Powmod(X86Microop):
        className = "UInt64PowmodMicroop"
        def __init__(self):
            pass

        def getAllocator(self, microFlags):
            return "new X86ISA::UInt64PowmodMicroop(machInst, macrocodeBlock, %s, 0)" % \
                    self.microFlagsText(microFlags)

    microopClasses["uint64_powmod"] = UInt64Powmod

    class DoublePow(X86Microop):
        className = "DoublePowMicroop"
        def __init__(self):
            pass

        def getAllocator(self, microFlags):
            return "new X86ISA::DoublePowMicroop(machInst, macrocodeBlock, %s, 0)" % \
                    self.microFlagsText(microFlags)

    microopClasses["double_pow"] = DoublePow

    class UInt64Fibmod(X86Microop):
        className = "UInt64FibmodMicroop"
        def __init__(self):
            pass

        def getAllocator(self, microFlags):
            return "new X86ISA::UInt64FibmodMicroop(machInst, macrocodeBlock, %s, 0)" % \
                    self.microFlagsText(microFlags)

    microopClasses["uint64_fibmod"] = UInt64Fibmod
}};